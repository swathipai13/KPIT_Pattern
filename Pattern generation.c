#include <stdio.h>
int main()
{
    int i, j, rows;

    printf("Enter number of rows");
    scanf("%d",&rows);

    for(j=1; j<=rows; ++j)
    {
        for(i=1; i<=i; ++i)
        {
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}
